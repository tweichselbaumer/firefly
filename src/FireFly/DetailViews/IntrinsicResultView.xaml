<UserControl x:Class="FireFly.DetailViews.IntrinsicResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FireFly.DetailViews"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Viewbox Grid.Row="0" Grid.Column="0">
            <Border BorderThickness="1" BorderBrush="Black">
                <Image Width="{Binding Path=Parent.CameraViewModel.ImageWidth}" Height="{Binding Path=Parent.CameraViewModel.ImageHeight}" Source="{Binding Path=ChAruCoBoard.ImageSource}" />
            </Border>
        </Viewbox>

        <StackPanel Grid.Row="0" Grid.Column="2" Grid.RowSpan="2">
            <StackPanel.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Export to Clipboard..." Command="{Binding Path=ExportToClipboardCommand}" />
                </ContextMenu>
            </StackPanel.ContextMenu>
            <Label Grid.Row="0" Grid.Column="0" Content="Camera Matrix" Style="{DynamicResource DescriptionHeaderStyle}" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="{Binding Path=Fx}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="0" Grid.Column="1" Content="0" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="0" Grid.Column="2" Content="{Binding Path=Cx}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />

                <Label Grid.Row="1" Grid.Column="0" Content="0" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="1" Grid.Column="1" Content="{Binding Path=Fy}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="1" Grid.Column="2" Content="{Binding Path=Cy}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />

                <Label Grid.Row="2" Grid.Column="0" Content="0" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="2" Grid.Column="1" Content="0" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="2" Grid.Column="2" Content="1" Style="{DynamicResource GeneralLabelStyle}" />
            </Grid>
            <Label Content="" Style="{DynamicResource GeneralLabelStyle}" />
            <Label Grid.Row="0" Grid.Column="0" Content="Distortion" Style="{DynamicResource DescriptionHeaderStyle}" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="{Binding Path=K1}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="0" Grid.Column="1" Content="{Binding Path=K2}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="0" Grid.Column="2" Content="{Binding Path=K3}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
                <Label Grid.Row="0" Grid.Column="3" Content="{Binding Path=K4}" ContentStringFormat="{}{0:F4}" Style="{DynamicResource GeneralLabelStyle}" />
            </Grid>
        </StackPanel>

        <Button Grid.Row="1" Grid.Column="0" Width="200" Content="calibrate/validate ChAruco" Style="{DynamicResource AccentedSquareButtonStyle}" Margin="0 5 0 5" Command="{Binding Path=StartCalibrationChArucoCommand}" />

        <Button Grid.Row="1" Grid.Column="2" Width="200" Content="calibrate AprilGrid" Style="{DynamicResource AccentedSquareButtonStyle}" Margin="0 5 0 5" Command="{Binding Path=StartCalibrationAprilGridCommand}" />
    </Grid>
</UserControl>